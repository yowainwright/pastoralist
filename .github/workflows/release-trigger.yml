name: pastoralist trigger release tests 🧑‍🌾

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [24.x]

    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v4.0.1
        with:
          token: ${{ secrets.REPO_ACCESS_TOKEN }}
          repository: yowainwright/pastoralist-test
          event-type: pastoralist_update
