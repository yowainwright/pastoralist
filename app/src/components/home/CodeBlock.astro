---
import { resolveDocsUrl } from "../../utils/urlResolver";
import '../../styles/terminal.css';
---

<div class="py-16 lg:py-24 bg-base-200/50">
  <div class="xl:flex gap-16 items-center max-w-2xl md:max-w-6xl mx-auto px-4">
    <div class="xl:max-w-xl flex flex-col justify-center">
      <h2 class="text-3xl lg:text-4xl font-black">
        <span class="gradient-text">Zero Maintenance</span> Override Tracking
      </h2>

      <p class="mt-6 text-lg text-base-content/80">
        Pastoralist automatically creates an appendix that documents <em>why</em> each
        override exists. Track which packages depend on each override, detect security
        fixes, and clean up stale overrides when they're no longer needed.
      </p>

      <ul class="mt-6 space-y-3 text-base-content/80">
        <li class="flex items-start gap-3">
          <span class="text-success mt-1">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Automatically tracks override dependencies</span>
        </li>
        <li class="flex items-start gap-3">
          <span class="text-success mt-1">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Documents security fixes with CVE references</span>
        </li>
        <li class="flex items-start gap-3">
          <span class="text-success mt-1">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Cleans up orphaned overrides automatically</span>
        </li>
        <li class="flex items-start gap-3">
          <span class="text-success mt-1">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Works with npm, yarn, pnpm, and bun</span>
        </li>
      </ul>

      <div class="flex gap-4 mt-8">
        <a href={resolveDocsUrl('introduction')} class="btn btn-lg gradient-bg text-white border-0">
          Learn More
        </a>
        <a href="https://github.com/yowainwright/pastoralist" class="btn btn-lg btn-ghost">
          View on GitHub
        </a>
      </div>
    </div>

    <div class="flex-1 mt-8 xl:mt-0">
      <!-- Simple postinstall setup example -->
      <div class="mockup-code text-sm">
        <pre data-prefix="1"><code><span class="text-base-content/50">// Add to package.json scripts</span></code></pre>
        <pre data-prefix="2"><code>{`{`}</code></pre>
        <pre data-prefix="3"><code>  <span class="text-primary">"scripts"</span>: {`{`}</code></pre>
        <pre data-prefix="4"><code>    <span class="text-primary">"postinstall"</span>: <span class="text-success">"pastoralist"</span></code></pre>
        <pre data-prefix="5"><code>  {`}`}</code></pre>
        <pre data-prefix="6"><code>{`}`}</code></pre>
        <pre data-prefix=" "><code></code></pre>
        <pre data-prefix="$"><code>bun install</code></pre>
        <pre data-prefix=" "><code><span class="text-cyan-400">👩🏽‍🌾 pastoralist checking herd...</span></code></pre>
        <pre data-prefix=" "><code><span class="text-success">👩🏽‍🌾 pastoralist the herd is safe!</span></code></pre>
      </div>
    </div>
  </div>
</div>
