---
import { resolveDocsUrl } from "../../utils/urlResolver";
import { CodeBlockToggle } from "./CodeBlockToggle";
import '../../styles/terminal.css';
---

<div class="py-16 lg:py-24 bg-base-200/50 border-y border-base-content/10">
  <div class="xl:flex gap-16 items-center max-w-2xl md:max-w-6xl mx-auto px-4">
    <div class="xl:max-w-xl flex flex-col justify-center">
      <h2 class="text-3xl lg:text-4xl font-black">
        <span class="gradient-text">Simple</span> Override Tracking
      </h2>

      <p class="mt-6 text-lg text-base-content/80">
        Pastoralist creates an appendix that documents <em>why</em> each
        override exists. Track which packages depend on each override, detect security
        fixes, and clean up stale overrides when they're no longer needed.
      </p>

      <ul class="mt-6 divide-y divide-base-content/10 border-y border-base-content/10 text-base-content/80">
        <li class="flex items-start gap-3 py-3">
          <span class="check-icon mt-0.5">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Tracks override dependencies</span>
        </li>
        <li class="flex items-start gap-3 py-3">
          <span class="check-icon mt-0.5">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Documents security fixes with CVE references</span>
        </li>
        <li class="flex items-start gap-3 py-3">
          <span class="check-icon mt-0.5">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Cleans up orphaned overrides</span>
        </li>
        <li class="flex items-start gap-3 py-3">
          <span class="check-icon mt-0.5">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span>Works with npm, yarn, pnpm, and bun</span>
        </li>
      </ul>

      <div class="flex gap-4 mt-8">
        <a href={resolveDocsUrl('introduction')} class="btn btn-lg btn-primary">
          Learn More
        </a>
        <a href="https://github.com/yowainwright/pastoralist" class="btn btn-lg btn-ghost">
          View on GitHub
        </a>
      </div>
    </div>

    <div class="flex-1 mt-8 xl:mt-0">
      <CodeBlockToggle client:only="react" height="340px" />
    </div>
  </div>
</div>
