---
import SpotlightCodeBlock from "./SpotlightCodeBlock";
import { resolveDocsUrl } from "../../utils/urlResolver";
---

<div class="xl:flex pt-0 sm:pt-8 lg:pt-12 pb-12 pb-8 lg:pb-20 gap-8 lg:gap-16 xl:gap-28 px-4 sm:px-6 lg:px-8">
  <div class="w-full max-w-3xl mt-6 sm:mt-8 xl:mt-0 order-2 xl:order-1 flex-1">
    <SpotlightCodeBlock client:load />
  </div>

  <div class="xl:max-w-xl flex flex-col justify-center font-outfit order-1 xl:order-2">
    <h1 class="mt-8 xl:mt-0 text-3xl sm:text-4xl lg:text-5xl font-black leading-tight">
      Works <span class="bg-clip-text text-transparent" style="background: linear-gradient(90deg, rgba(61, 4, 217, 1) 2%, rgba(114, 4, 217, 1) 55%, rgba(137, 10, 196, 1) 100%); -webkit-background-clip: text;">Seamlessly</span> with Your Workflow
    </h1>

    <p class="mt-4 sm:mt-6 text-base sm:text-lg">
      Pastoralist integrates perfectly into your development process. Run it as part of
      your CI/CD pipeline, include it in your pre-commit hooks, or use it manually
      whenever you need to update your dependency overrides.
    </p>

    <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 mt-6 sm:mt-8">
      <a href={resolveDocsUrl("introduction")} class="w-full sm:w-auto">
        <button class="btn btn-lg sm:btn-md text-white border-0 hover:opacity-90 w-full sm:w-auto" style="background: linear-gradient(90deg, rgba(61, 4, 217, 1) 2%, rgba(114, 4, 217, 1) 55%, rgba(137, 10, 196, 1) 100%);"> Get Started </button>
      </a>

      <a href={resolveDocsUrl("setup")} class="w-full sm:w-auto">
        <button class="btn btn-lg sm:btn-md btn-outline w-full sm:w-auto"> View Setup Guide </button>
      </a>
    </div>
  </div>
</div>
