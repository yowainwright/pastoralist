import { Mermaid } from "mdx-mermaid/Mermaid";
import ReviewFlow from "../Components/ReviewFlow";

<section>

### 1. Review

When Pastoralist is run once, it creates a pastoralist object which maps `resolutions` to `pastoralist.appendix`.

When Pastoralist is run again, it reviews the `pastoralist.appendix` as well as resolutions and updates the `pastoralist.appendix` accordingly.

#### The flow graph below outlines this process

<ReviewFlow />

---

</section>
